import Body from "../body/body";

const Home = () => {
  const pageTitle = 'Jeedie React Dev';
  return (
    <div>
      <h1>{pageTitle}</h1>
      <Body></Body>
    </div>
  );
};

export default Home;
